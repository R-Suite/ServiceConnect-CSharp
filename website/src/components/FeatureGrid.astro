---
import { CardGrid, LinkCard } from '@astrojs/starlight/components';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<CardGrid>
  <LinkCard
    title="Pub/Sub"
    href={`${base}/learn/messaging-patterns/pub-sub/`}
    description="Broadcast messages to every interested subscriber without caring who listens."
  />
  <LinkCard
    title="Process Manager"
    href={`${base}/learn/messaging-patterns/process-manager/`}
    description="Coordinate long-running multi-step workflows with first-class state and timeouts."
  />
  <LinkCard
    title="Routing Slip"
    href={`${base}/learn/messaging-patterns/routing-slip/`}
    description="Attach an itinerary to a message so each step knows exactly where it goes next."
  />
  <LinkCard
    title="Scatter/Gather"
    href={`${base}/learn/messaging-patterns/scatter-gather/`}
    description="Fan out a request to many handlers and aggregate their replies into one response."
  />
  <LinkCard
    title="Aggregator"
    href={`${base}/learn/messaging-patterns/aggregator/`}
    description="Batch related messages by key and correlation window, then publish the collected result."
  />
  <LinkCard
    title="Filters"
    href={`${base}/learn/messaging-patterns/filters/`}
    description="Stamp, compress, dedupe, or redact messages with a pluggable cross-cutting hook."
  />
</CardGrid>
